# 纪念日网页

一个浪漫的纪念日网页，包含打印机效果的文字输出、背景音乐和倒计时功能。

## 功能特点

- ✨ 打印机效果的文字输出
- 🎵 背景音乐自动播放（可设置延迟时间）
- ⏰ 纪念日倒计时
- 💌 信封打开动画效果
- 🎈 背景告白气球特效

## 使用教程

### 安装依赖

```bash
npm install
```

### 启动项目

```bash
npm start
```

### 个性化设置

#### 1. 修改纪念日期

打开 `src/Main.js`，找到 `componentDidMount()` 方法中的日期设置：

```javascript
this.time(2016, 4, 23) // 修改为你们的纪念日（年, 月, 日）
```

#### 2. 修改背景音乐

1. 将音乐文件（推荐 mp3 格式）放到 `src/audio/` 目录下
2. 在 `src/Main.js` 中修改导入路径：

```javascript
import url from './audio/your-music.mp3'
```

3. 修改延迟播放时间（毫秒）：

```javascript
setTimeout(() => audio.play(), 8500) // 修改延迟时间
```

#### 3. 修改网页标题

打开 `public/index.html`，修改：

```html
<title>两周年纪念！</title>
```

#### 4. 修改信封标题

打开 `src/App.js`，修改：

```javascript
<div className="text">你收到一封信，点击查收！</div>
```

#### 5. 修改文章内容

打开 `src/Main.js`，在 `render()` 方法中找到 `#autotype` 部分，修改文字内容。

### 打包编译

```bash
npm run build
```

打包后的文件在 `build` 目录下。

## 技术栈

- React
- create-react-app
- jQuery (用于动画效果)

## 部署

### 部署到 Vercel（推荐）

1. 安装 Vercel CLI（如果还没有）：
```bash
npm i -g vercel
```

2. 在项目根目录运行：
```bash
vercel
```

3. 或者通过 Vercel 网站：
   - 访问 [vercel.com](https://vercel.com)
   - 导入 GitHub 仓库
   - 自动检测并部署

### 其他部署方式

也可以部署到：
- GitHub Pages
- Netlify
- 或其他静态网站托管服务

